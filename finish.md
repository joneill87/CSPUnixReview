# Done

In the last forty minutes you have found your way around a filesystem
using relative paths, moved and copied and removed things, read a log
to work out why a service failed, traced a port to the process holding
it, fixed and compiled a C program, and written a script that takes an
argument.

All six come back in week one, when the placeholder on port 80 is
replaced by something you wrote.

If any step needed its tip, that is the topic worth half an hour with
`man` before the first lab.
